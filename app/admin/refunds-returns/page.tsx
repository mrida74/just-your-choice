import RefundReturnManager from "@/components/admin/RefundReturnManager";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function AdminRefundsReturnsPage() {
  return <RefundReturnManager />;
}
